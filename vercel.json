{
  "buildCommand": "cd packages/web && bunx vite build && (cp -f ../health-contract/health-contract.wasm dist/health-contract.wasm 2>/dev/null || true)",
  "outputDirectory": "packages/web/dist",
  "installCommand": "bun install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [{ "key": "x-powered-by", "value": "T3-MedAgent" }]
    }
  ]
}
